void snake(){
	while (1){
		clear();
		matrix[0][0] = 1;
		update();
	}

	/* int len{1}, x{3}, y{3}, dir{}, last{}; */

	/* while (1){ */
	/* 	int input = joystick_event(); */
	/* 	if (input % 2 != dir % 2) */
	/* 		dir = input; */

	/* 	clear(); */
	/* 		// do update s tuff */
	/* 	update(); */
	/* } */
}
